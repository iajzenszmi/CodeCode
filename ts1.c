int main(){         
struct Data
         {
         
         Data() { for (int i = 0; i < 64; ++i)
        values[i] = i; }
        int values[64];
        };

        Data* data = new Data; 
        } 
